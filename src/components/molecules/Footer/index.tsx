import { ReactElement } from 'react';

const Footer = (): ReactElement => {
    return (
        <footer className="flex items-center justify-center min-h-[5vh]">
            Contact m.coughlan18@gmail.com with any feedback
        </footer>
    );
};

export default Footer;
